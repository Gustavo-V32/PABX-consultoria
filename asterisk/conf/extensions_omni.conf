; ============================================================
; OmniSuite generated file: extensions_omni.conf
; Generated at: 2026-05-14T19:34:49.600Z
; Do not edit manually. Changes are overwritten by PABX Admin.
; ============================================================

[omni-from-internal]
exten => _X.,1,Goto(from-internal,${EXTEN},1)

[from-internal-custom]
exten => 1,1,Verbose(1,OmniSuite ramal 1)
 same => n,Dial(SIP/1,30,tTkK)
 same => n,Hangup()

[omni-inbound]
exten => 556130606738,1,Verbose(1,OmniSuite rota entrada Entrada DID 556130606738)
 same => n,Set(CHANNEL(language)=pt_BR)
 same => n,Goto(omni-ivr-62d3ec22,s,1)

exten => _X.,1,Verbose(1,OmniSuite entrada sem rota especifica)
 same => n,Goto(ivr-main,s,1)

[omni-outbound]
exten => _9X.,1,Verbose(1,OmniSuite saida NOVACIA via prefixo 9: ${CALLERID(num)} -> ${EXTEN:1})
 same => n,Set(CDR(userfield)=external_outbound)
 same => n,Dial(SIP/omni_trunk_novacia_6130606738/${EXTEN:1},60,tTkK)
 same => n,Hangup()

exten => _0X.,1,Verbose(1,OmniSuite saida NOVACIA nacional/internacional: ${CALLERID(num)} -> ${EXTEN})
 same => n,Set(CDR(userfield)=external_outbound)
 same => n,Dial(SIP/omni_trunk_novacia_6130606738/${EXTEN},60,tTkK)
 same => n,Hangup()

exten => _[2-9]XXXXXXX.,1,Verbose(1,OmniSuite saida NOVACIA direta: ${CALLERID(num)} -> ${EXTEN})
 same => n,Set(CDR(userfield)=external_outbound)
 same => n,Dial(SIP/omni_trunk_novacia_6130606738/${EXTEN},60,tTkK)
 same => n,Hangup()

[omni-ivr-62d3ec22]
exten => s,1,Answer()
 same => n,Wait(1)
 same => n,Verbose(1,URA URA Principal 556130606738: Digite 1 para suporte, 2 para vendas, 3 para financeiro ou 0 para recepcao.)
 same => n,WaitExten(8)
 same => n,Goto(t,1)
exten => 0,1,Verbose(1,Opcao URA Recepcao)
 same => n,Queue(omni_q_recep_o_geral,tTHhc,,,300)
 same => n,Hangup()
exten => 1,1,Verbose(1,Opcao URA Suporte)
 same => n,Queue(omni_q_suporte_t_cnico,tTHhc,,,300)
 same => n,Hangup()
exten => 2,1,Verbose(1,Opcao URA Vendas)
 same => n,Queue(omni_q_vendas,tTHhc,,,300)
 same => n,Hangup()
exten => 3,1,Verbose(1,Opcao URA Financeiro)
 same => n,Queue(omni_q_financeiro,tTHhc,,,300)
 same => n,Hangup()
exten => i,1,Verbose(1,Opcao URA Opcao invalida recepcao)
 same => n,Queue(omni_q_recep_o_geral,tTHhc,,,300)
 same => n,Hangup()
exten => t,1,Verbose(1,Opcao URA Timeout recepcao)
 same => n,Queue(omni_q_recep_o_geral,tTHhc,,,300)
 same => n,Hangup()
exten => h,1,Hangup()
