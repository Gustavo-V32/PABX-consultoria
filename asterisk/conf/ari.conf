; ============================================================
; OmniSuite - Asterisk REST Interface (ARI)
; ============================================================

[general]
enabled=yes
pretty=no
allowed_origins=*
auth_realm=Asterisk REST Interface

[omni_ari]
type=user
read_only=no
password=REPLACE_ARI_SECRET
password_format=plain
